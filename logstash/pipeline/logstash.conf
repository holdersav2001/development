input {
  kafka {
    bootstrap_servers => "broker:29092"
    topics => ["cdc.trades.trade_event"]
    codec => json
  }
}

filter {
  json {
    source => "message"
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "cdc_trades_trade_event"
  }
}
